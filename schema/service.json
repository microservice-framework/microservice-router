{
  "id": "/Service",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "Microservice type. Default is handler.",
      "enum": ["hook", "handler", "websocket", "metric"],
      "default": "handler"
    },
    "hook": {
      "type": "array",
      "description": "Only for hook. Supported hooks.",
      "items": {
        "type": "object",
        "properties": {
          "phase": {
            "type": "string",
            "enum": ["before", "after"],
            "required": true
          },
          "type": {
            "type": "string",
            "enum": ["adapter", "notify", "broadcast"],
            "required": true
          },
          "group": {
            "type": "string",
            "description": "Group for notify and adapter. NOTIFY will be send one per group. Order is natural sorting."
          }
        }
      },
      "minItems": 1
    },
    "conditions": {
      "type": "object",
      "properties": {
        "headers": {
          "type": "array",
          "description": "Header matching conditions.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Header name",
                "required": true
              },
              "value": {
                "type": "string",
                "description": "Header value",
                "required": true
              },
              "isRegex": {
                "type": "boolean",
                "description": "Header value is regex to match"
              }
            }
          },
          "minItems": 1
        },
        "methods": {
          "type": "array",
          "description": "Request method matching conditions.",
          "items": {
            "type": "string",
            "enum": ["GET", "POST", "PUT", "DELETE", "SEARCH", "OPTIONS"]
          },
          "minItems": 1
        },
        "payload": {
          "type": "array",
          "description": "Data conditions.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "Field name of the object",
                "required": true
              },
              "value": {
                "type": ["string", "boolean", "number"],
                "description": "Field value",
                "required": true
              },
              "isRegex": {
                "type": "boolean",
                "description": "Field value is regex to match"
              }
            }
          },
          "minItems": 1
        }
      }
    },
    "scope": {
      "type": "string",
      "description": "Scope to provide access by access token."
    },
    "online": {
      "type": "boolean",
      "description": "Is service online or offline. For CI/CD."
    },
    "path": {
      "type": "array",
      "description": "Array of supported path.",
      "items": {
          "type": "string"
      },
      "minItems": 1
    },
    "url": {
      "type": "string",
      "description": "url to access service http://ip:port.",
      "required": true
    }
  }
}
